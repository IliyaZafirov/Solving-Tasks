// function mathPow(num, power) {
//     console.log(Math.pow(num, power));
// }

function solve(value, power) {

    let mathPow = (num, poweredBy) => Math.pow(num, poweredBy);

    console.log(mathPow(value,power));
    
}

solve(2, 8)